<?xml version="1.0" encoding="utf-8"?>

<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/circle_round20"
    android:backgroundTint="@color/white0"
    tools:context=".room.view.CreateRoomActivity">

    <ImageView
        android:id="@+id/ivClose"
        android:background="@color/white0"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_margin="10dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_icon_clear" />

    <mashup.loling.room.BlockSwipeViewPager
        android:id="@+id/createRoomViewPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="66dp">

    </mashup.loling.room.BlockSwipeViewPager>
</android.support.constraint.ConstraintLayout>